# Additional Materials: Bank Branch Closures and Neighbourhood Characteristics

## 1. Purpose

This folder contains the data, code, spatial files and saved outputs needed to reproduce the reported area-level and branch-level analyses. The workflow combines England and Wales Census 2021 data, Scotland Census 2022 data, Geolytix bank-branch records, a postcode-to-small-area lookup and a combined Great Britain boundary file. It then produces two analytical CSV files, exploratory analysis, maps, Ball Mapper graphs and robustness checks.

The materials are organised around one rule: a reader should be able to start from the supplied source files, run the notebooks in the stated order and recover the analytical datasets and reported results without changing machine-specific paths.

## 2. Important redistribution note

The local submission folder includes copies of the source datasets because they were requested for the assessment package. Inclusion here does **not** automatically establish that every file may be redistributed through a public GitHub repository.

Before publishing this folder publicly:

1. check the current licence and attribution requirements for every source;
2. pay particular attention to postcode products and spatial boundary components that may contain third-party intellectual property;
3. if redistribution is restricted or the repository becomes impractically large, remove the affected source file and retain its official access link, exact filename, required columns, directory location and preparation instructions;
4. keep the processing code unchanged so that a lawful local copy can be placed in the documented directory and used directly.

The Geolytix banking release was announced as open data under CC0. Census statistics are published by ONS and National Records of Scotland, but users should still retain the required Crown copyright or Open Government Licence attribution. The prepared postcode lookup and combined boundary file should be checked against the licences of the products from which they were derived before public release.

## 3. Folder structure

```text
Additional_Materials_Submission/
|-- README.md
|-- requirements.txt
|-- environment.yml
|-- FILE_MANIFEST.csv
|-- notebooks/
|   |-- data_matching_procedure.ipynb
|   |-- 01_area_level_EDA.ipynb
|   |-- 02_area_level_TDABM_robustness_pyballmapper.ipynb
|   |-- 03_bank_level_EDA.ipynb
|   |-- 04_bank_level_TDABM_robustness_pyballmapper.ipynb
|   `-- tdabm_method_appendix_figures.ipynb
|-- data/
|   |-- raw/
|   |   |-- census_england_wales/
|   |   |-- census_scotland/
|   |   `-- bank/
|   |-- reference/
|   |   `-- postcode_lookup/
|   `-- processed/
|       |-- eda_area_level.csv
|       `-- eda_bank_level.csv
|-- spatial/
|   `-- GB_LSOA_DZ_2022.{shp,shx,dbf,prj,cpg}
|-- results/
|   |-- figures/
|   |-- eda_tables/
|   `-- tdabm_tables/
`-- documentation/
    `-- References.bib
```

`results/figures` contains the figures cited in the report together with the supporting TDABM colour maps and robustness figures generated by notebooks 02 and 04. `results/eda_tables` and `results/tdabm_tables` contain machine-readable versions of the reported summaries and robustness outputs. The simplified GeoPackage created during mapping is a cache and is deliberately omitted because notebooks 01 and 03 can regenerate it from the supplied shapefile.

## 4. Software environment

The analysis was prepared using Python 3.13.5. Exact package versions are listed in `requirements.txt` and `environment.yml`. The principal packages are pandas, NumPy, Matplotlib, seaborn, GeoPandas, Shapely, SciPy, scikit-learn, NetworkX, openpyxl and pyballmapper.

Create the environment with either of the following approaches.

### Conda

```bash
conda env create -f environment.yml
conda activate erp-tdabm
jupyter lab
```

### pip

```bash
python -m venv .venv
.venv\Scripts\activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
jupyter lab
```

On macOS or Linux, activate the virtual environment with `source .venv/bin/activate`.

## 5. Run order

Open Jupyter from the package root and run the notebooks in this order.

### Step 1: `notebooks/data_matching_procedure.ipynb`

Reads all Census, bank and postcode files from `data/raw` and `data/reference`. It standardises area identifiers, reshapes the Census source tables, checks one-to-one area merges, normalises postcodes, attaches bank records to their host areas and writes:

- `data/processed/eda_area_level.csv`
- `data/processed/eda_bank_level.csv`

Expected saved dimensions are 43,064 rows by 69 columns for the area file and 11,564 rows by 87 columns for the branch file. The area file should contain one row per area code. The branch file should contain one row per bank-record identifier.

### Step 2: `notebooks/01_area_level_EDA.ipynb`

Reads both processed CSV files, constructs percentages and area-level bank-status measures, writes descriptive tables to `results/eda_tables`, and writes area-level distributions and maps to `results/figures`. The supplied combined boundary file is read from `spatial`.

### Step 3: `notebooks/02_area_level_TDABM_robustness_pyballmapper.ipynb`

Builds the area-level neighbourhood-characteristics space from 30 Census features. Banking outcomes are used for colouring and do not determine distance or ball membership. The main manually selected radius is 7.2, and the baseline landmark seed is 42. The notebook also contains reduced-variable and expanded-variable checks, followed by a 50% majority-rule landmark-order check. The 10,000-seed procedure is computationally expensive and may require several hours depending on hardware.

### Step 4: `notebooks/03_bank_level_EDA.ipynb`

Produces branch-record descriptive tables, distributions and maps. Open, closed and closing records are retained for descriptive checks. TDABM later uses realised open and closed records for the main branch comparison.

### Step 5: `notebooks/04_bank_level_TDABM_robustness_pyballmapper.ipynb`

Builds the branch-level neighbourhood-characteristics space from the same 30 Census features. The main manually selected radius is 7.0, and the baseline landmark seed is 42. It repeats the reduced-variable, expanded-variable and 50% majority-rule landmark-order checks used in the area analysis.

### Step 6: `notebooks/tdabm_method_appendix_figures.ipynb`

Generates the synthetic diagrams used to explain greedy cover construction, conversion of the overlapping cover into a graph, and the effect of changing epsilon. It does not use the empirical Census or branch records and may be run independently.

Notebooks can be opened from either the package root or the `notebooks` directory. All paths are resolved relative to the package root. No code refers to the author's original `C:\Users\...` location.

## 6. Analytical levels and outcomes

The two datasets answer different questions.

- **Area level:** one observation is an LSOA in England or Wales or a Data Zone in Scotland. Among previously observed banking areas, `complete withdrawal` indicates closed records without a remaining open record, while `retained provision` indicates that at least one open record remains.
- **Branch level:** one observation is a matched bank record. The principal comparison is between records labelled `Open` and `Closed`. A closed branch does not necessarily imply complete withdrawal from its host area because another branch may remain there.

These labels should not be interchanged. `Complete withdrawal` and `retained provision` describe area-level banking histories. `Open` and `closed` describe individual branch records.

## 7. TDABM feature space

The same 30 Census features define distance at both analytical levels:

- age: 0--15, 16--24, 25--64 and 65-plus;
- household deprivation: zero, one and two-or-more dimensions;
- car availability: zero, one and two-or-more cars;
- health and disability: very good health, good health, other health and disability;
- qualifications: low, middle and higher groups;
- NS-SEC: managerial/professional/small-employer, intermediate/lower-supervisory, semi-routine/routine, and never-worked/long-term-unemployed groups;
- economic activity: employed, unemployed, retired, long-term sick or disabled, and other inactive groups;
- industry: production, distribution/transport, finance/real-estate/professional/administrative, and public/other services.

Each feature is standardised before Euclidean distance is calculated. Ball size records membership count, an edge records at least one shared observation, and colour records a node-level mean. Graph coordinates are layout coordinates and must not be interpreted as geographical locations or measured socio-economic distances.

## 8. Source data inventory

### England and Wales Census 2021

Directory: `data/raw/census_england_wales`

| File | Topic |
|---|---|
| `census2021-ts007-lsoa-age 5 years.csv` | Age |
| `census2021-ts011-lsoa-depreviation.csv` | Household deprivation dimensions |
| `census2021-ts037-lsoa-General Health.csv` | General health |
| `census2021-ts038-lsoa-disability.csv` | Disability |
| `census2021-ts045-lsoa-car.csv` | Car or van availability |
| `census2021-ts060-lsoa-industry.xlsx` | Industry |
| `census2021-ts062-lsoa-NS-SEC.csv` | National Statistics Socio-economic Classification |
| `census2021-ts066-lsoa-economic activity.csv` | Economic activity |
| `census2021-ts067-lsoa-education.csv` | Qualifications |

Official access point: <https://www.nomisweb.co.uk/sources/census_2021>

### Scotland Census 2022

Directory: `data/raw/census_scotland`

The nine Excel files cover age, car availability, deprivation, disability, economic activity, education, general health, industry and NS-SEC at Data Zone level.

Official data-zone access point: <https://www.scotlandscensus.gov.uk/documents/2022-data-zone-area-data/>

### Geolytix bank records

File: `data/raw/bank/geolytix_uk_open_bank_branches.csv`

The file records branch identifiers, brands, addresses, coordinates, status and closure information used in the branch match and banking-history construction. Geolytix described the banking release as open data under CC0:

<https://geolytix.medium.com/bank-branch-data-open-and-free-from-geolytix-795a9339a20d>

Retain the source filename and the access date used for the submitted analysis. A later download may contain revisions or additional closures and therefore need not reproduce the submitted counts exactly.

### Postcode-to-LSOA lookup

File: `data/reference/postcode_lookup/Prepared pcd to lsoa.csv`

Only the `pcds` and `lsoa21cd` fields are read by the matching notebook. The file is a prepared lookup used to attach normalised bank postcodes to 2021 LSOAs or equivalent small-area codes. ONS postcode products are released quarterly:

<https://www.ons.gov.uk/methodology/geography/geographicalproducts/postcodeproducts>

Because postcode products can contain Royal Mail, Gridlink, Ordnance Survey and other intellectual property, verify the exact source edition and redistribution conditions before placing this prepared file in a public repository.

### Spatial boundary

Files: `spatial/GB_LSOA_DZ_2022.shp`, `.shx`, `.dbf`, `.prj` and `.cpg`

All five components are required. They form one combined plotting layer for England and Wales LSOAs and Scottish Data Zones. Do not rename or separate individual components. The layer is used only for mapping; TDABM distance is calculated from Census features, not polygon geometry.

## 9. Saved outputs

- `results/figures` contains report figures, the complete Census-coloured Ball Mapper series, epsilon scans, selected-ball profiles and robustness plots.
- `results/eda_tables` contains descriptive and country/status summaries produced by notebooks 01 and 03.
- `results/tdabm_tables` contains feature definitions, epsilon-selection results, ball summaries, selected-ball extracts and 50% majority-rule landmark-order robustness outputs.

Saved outputs are included to help a reviewer compare a fresh run with the submitted analysis. They are not substitutes for the code. If a notebook is rerun, review file timestamps and compare the new tables with the supplied versions before replacing them.

## 10. Reproduction checks

After running `data_matching_procedure.ipynb`, confirm the following before proceeding:

1. all 20 declared input paths exist;
2. the area file has 43,064 rows and unique `area_code` values;
3. the branch file has 11,564 rows and unique `id` values;
4. the postcode match does not multiply branch records;
5. the country assignment and bank-status counts reconcile with the notebook outputs;
6. notebooks 01 and 03 find `spatial/GB_LSOA_DZ_2022.shp` and generate the map cache successfully;
7. notebooks 02 and 04 report 30 `BM_FEATURES` and the intended manual epsilon values;
8. the main area and branch graphs, ball summaries and robustness tables are written to `results`.

For a clean verification, copy the folder to a new location, create the environment, run the five empirical notebooks in order, run the method-figure notebook separately, and compare generated filenames and summary counts against `FILE_MANIFEST.csv` and the saved tables.

## 11. Troubleshooting

- **A raw file is reported missing:** check that its spelling and spaces match the inventory above. The original filenames are intentionally retained.
- **Excel import fails:** install the pinned `openpyxl` version.
- **GeoPandas cannot read the shapefile:** confirm that all five shapefile components are together and have the same base name.
- **A map cache appears stale:** remove only the generated `results/gb_lsoa_dz_2022_plot_simplified_18m.gpkg` file and rerun the relevant EDA notebook.
- **Ball Mapper is unavailable:** activate the documented environment and verify `pyballmapper==0.3.7`.
- **Robustness checks take a long time:** first run the main graph and low-cost diagnostics. Run the 10,000-seed cells only when the environment and selected epsilon are confirmed.
- **A public repository exceeds hosting limits:** keep code, configuration, README, tables and selected figures in GitHub, and replace large source files with lawful access instructions. Do not silently change the directory structure expected by the notebooks.

## 12. Document traceability

`documentation/References.bib` records the bibliography used by the report. The assessed report is submitted separately and is not duplicated in the additional-materials package.
